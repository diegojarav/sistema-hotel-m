[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    kpi: KPI evaluation tests (scored 0-100, output JSON reports)
    perf: Performance benchmark tests (measured against thresholds)
    slow: Tests that take >5 seconds
